experiment:
  name: smoke
  output_dir: results/smoke/{timestamp}
  epochs: 1
  batch_size: 16
  num_workers: 0
data:
  path: data/synth/data.csv
  format: csv
  label_col: label
  categorical_cols: ["cat_0", "cat_1"]
  group_col: null
  impute: mean
  standardize: true
  categorical_encoding: none
  stats_path: results/smoke/{timestamp}/preprocess_stats.json
